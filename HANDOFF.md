# HANDOFF.md（guitar-practice）

- **最終更新日時**: 2026-08-22（Claude追記）
- **変更主体**: Claude（ストロークの区切り・歌詞位置ズレ機能を追加）

## 直近の追記（2026-08-22）

ブロック先行方式（下記参照）の上に、実際に使ってみて出た要望を追加実装済み・push済み：

- **ストロークの区切り（空き）**: `STRUM_GAP`という特別な印を`block.strum`配列に混ぜられる。↓↑✕〜と並ぶ「␣」ボタンで手動挿入でき、保存済みストロークプリセットを2回目以降タップした時は自動で挟まる（同じパターンの繰り返しを視覚的な塊として見分けるため）。表示はどこも`spRenderStrumSpans`という共通ヘルパー経由（区切りは空白spanとして描く）。
- **歌詞がストロークの途中から始まる／途中で終わるズレ**: `block.lyricStart`（整数）で「歌詞が乗り始める記号の添字」を指定できる。ブロック追加フォームでストロークの記号自体をタップすると設定される（ハイライト表示）。演奏画面側の判定は`lyricEnd = lyricStart + 歌詞文字数`が`strum.length`に収まっていればOK、という一般化した条件にしてあり、前だけ・後ろだけ・両方に「歌詞なしでストロークだけ弾く空き」を作れる。
- **既知の未解決事項（次回への引き継ぎ）**: 編集画面の「ブロック一覧」（`spRenderBlockList`）は、ストローク欄と歌詞欄を別々にそのまま表示しているだけで、`lyricStart`によるズレを**反映していない**（ただの左揃えの2行）。実際にズレが正しく効くのは演奏画面（`spBuildStrumLyricRows`）だけ。一覧側もズレを反映させるかどうか、ユーザーに確認中で未回答（「一覧はざっくり表示のままでいい」か「一覧も正確な位置合わせにする」か）。次回はここから再開する。
- 途中で見つけて直したバグ2件: (1)貼り戻し機能が古いスキーマの変数`tags`を参照していてエラーになっていた（`blocks`に修正済み）、(2)`STRUM_GAP`定数の文字列リテラル内に編集時の事故でNULLバイトが混入していた（直接バイト修正で解消）。

以下は2026-08-20時点の内容（ブロック先行方式への刷新）。

このファイルは長期の設計方針（`CLAUDE.md`）ではなく、今どこまで進んでいて次に何をするかの短期メモ。ClaudeとCodexの併用運用の一環（詳細はルート`CLAUDE.md`の「Claude/Codexの併用について」、`Obsidian Vault\メモ\Claude-Codex併用ルール.md`を参照）。**このリポジトリには`AGENTS.md`もあるので先に読んでください。**

## 今回の目的

U-FRET（https://ufret.jp）のような「歌詞の上にコードを表示しながら弾く」機能を、既存の「曲」タブに追加する。アイデアメモ（Obsidian Vault/メモ/アイデア.md）の01番の要望が発端。2026-08-14に本実装まで完了・実機確認済み。2026-08-18〜20に、ユーザーが実際に参考にしている動画（フレット図＋ストローク矢印＋歌詞が並び、現在位置がハイライトされる、U-FRET系のスタイル）に合わせて、**データモデルと編集画面を根本から作り直した**。

**現在の状態: 実装・PC上での機能テスト完了、push済み。実機での最終確認は次回。**

## 現在のデータモデル（2026-08-20時点、これが最新）

`USER_SONGS`（localStorageキー`guitar-user-songs`、プロフィール非依存）の各曲は：

```
{ id, title, artist, progression, blocks: [
    { chord, strum: string[], lyric: string, lineBreakBefore: boolean }, ...
] }
```

**設計の変遷（重要、同じ勘違いを繰り返さないための記録）**:
1. 最初は「歌詞をテキストとして先に全部書いて、文字位置にコードをタグ付けする」方式だった（`lyrics: string[]` + `tags: {line,ch,chord}[]`）。
2. しかしユーザーが実際に参考にしていたのは、U-FRETや弾き語り動画にあるような「**コード進行とストロークがまずあって、そこに歌詞が乗る**」スタイルだった。歌詞ベースでコードを後付けする発想とは逆。
3. そのため2026-08-20に、曲データを**ブロック（コード＋ストローク＋そのブロック専用の短い歌詞）の並び**として持つ形に全面刷新した。`progression`は`blocks.map(b=>b.chord)`から自動導出。

**ストロークの表現方法**: `block.strum`は文字列の配列。**歌詞`block.lyric`の文字数とぴったり同じ長さなら「1文字ずつ個別のストローク」として歌詞の真上に1:1で表示、一致しなければ「コード全体で1パターン」として1行でまとめて表示**する。この判定は表示側（`spBuildStrumLyricRows`）の長さ比較だけで行っており、データ構造自体に「個別か全体か」のフラグは持たせていない。

## 編集画面（`sp-edit-overlay`、2026-08-20全面刷新）

- 「＋ブロックを追加」→コードをパレットから選ぶ→↓/↑/✕/〜ボタンでストロークを組み立てる→そのブロックに乗る歌詞をテキスト欄に入力→「ここで行を変える」（`lineBreakBefore`）を必要なら付ける、で1ブロックずつ組み立てる（`block-form-modal`）。
- 組み立て済みのブロックは一覧（`sp-block-list`）に表示され、タップすると同じフォームで編集・削除できる。
- 以前あった「歌詞テキストエリア＋タップで文字位置選択」の入力方式・その関連コード（`spBuildLineEl`, `spCommitLyrics`, `spSelected`/`spSelectedTag`等）は全て削除済み。

## 演奏画面（`sp-play-overlay`）

- 曲全体ぶんのブロックを、既存の「流れる」タブと同じ考え方でタイルとして一括描画し、折り返しながら並べて縦スクロールで見る（U-FRET風）。`lineBreakBefore`のブロックの手前に改行を挟む。
- 各カードは**フレット図（一番上）→ストローク→歌詞**の順。フレット図は**この画面専用に横向き(`drawFretH`)固定**（コード/流れるタブの縦横設定`fretIsHoriz`には影響されない独立設定。ユーザーからのフィードバックで「左から右に1フレット2フレット、上から下に1弦2弦」の向きが良いと確認済み）。
- 自動再生（タイマーで一定間隔ごとに1ブロック進める、秒指定/BPM+拍数指定の両モード）と手動送り（◀/▶ボタン、カード自体のタップでも直接ジャンプ可）を両方常に使える。曲の最後まで来たら自動的に止まる。

## 削除した機能: 参考動画添付（2026-08-18、追加した直後に撤回）

「演奏画面に動画を乗せたい」という要望と誤解して実装したが、意図は「動画のような見た目を自分たちのデータで再現したい」であり動画添付自体は要望されていなかった。git revertで削除済み（コミット`6b52b65`とそのrevert `3bfd2c3`。IndexedDBでの動画保存という設計自体は動作確認まで済んでいるので、将来的に本当に動画添付が必要になった時は参照可能）。

## 著作権への対応（変更なし）

- 歌詞データはユーザー本人が用意する。Claudeが歌詞を取得・転記することは一切していない。
- 曲データは`localStorage`止まりで、公開リポジトリにコミットされる`index.html`自体には実データを含めていない。

## 今後の課題（未着手）

1. 全曲まとめての書き出し/読み込み（今は1曲ずつの「コピー」「貼り戻す」のみ）。
2. アクセシビリティ（タップ領域、読み上げ対応）。
3. ブロックの並べ替え（今は末尾追加・個別編集・個別削除のみ。途中に挿入したい場合は前後のブロックを一旦削除して作り直す必要がある）。
4. カードが切り替わる瞬間のアニメーション（今は瞬時に切り替わるだけ）。

## Codex活用状況（2026-08-14, 2026-08-18）

- 2026-08-14: `song-player-test.html`（プロトタイプ段階）の追加バグ調査を1回実施。8件の小さなバグをCodexが直接修正し、Claude側で確認済み。
- 2026-08-18: 参考動画添付機能の実装をCodexに委譲したが、**シェル実行禁止の制約で既存コードを検索できず、既存関数を直接編集せずモンキーパッチする方式になり、かつUI挿入先のCSSクラスが実在せず意図しない位置に挿入される**という問題が生じたため、Codexの差分は不採用・破棄。この機能自体も後に要望と異なると判明し撤回。**教訓: シェル禁止の制約下でCodexに大きめの機能追加を任せる場合、既存コードの構造（関数名・DOM構造・CSSクラス名）を事前にプロンプト内で具体的に示さないと、検索できない代わりに安全側だが構造の合わない実装になりやすい。**
- 2026-08-20のブロック方式全面刷新はClaudeが直接実装（Codex委譲なし）。

## 参照

- [CLAUDE.md](CLAUDE.md)
- [AGENTS.md](AGENTS.md)
- `index.html`（`SONGS`/`USER_SONGS`/`mergedSongs`/`songChordInfo`/`songChords`/`ALL_CHORDS`/`drawFretH`、`sp*`関数群が歌詞＋コード＋ストローク演奏機能一式。特に`spBuildStrumLyricRows`が1文字ずつ/全体パターンの判定ロジック）
- Obsidian Vault/メモ/アイデア.md（01番、今回の発端となったU-FRET関連の要望）
