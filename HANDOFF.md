# HANDOFF.md（guitar-practice）

- **最終更新日時**: 2026-08-14（Claude追記）
- **変更主体**: Claude（歌詞＋コード演奏画面機能の本実装・push）

このファイルは長期の設計方針（`CLAUDE.md`）ではなく、今どこまで進んでいて次に何をするかの短期メモ。ClaudeとCodexの併用運用の一環（詳細はルート`CLAUDE.md`の「Claude/Codexの併用について」、`Obsidian Vault\メモ\Claude-Codex併用ルール.md`を参照）。**このリポジトリには`AGENTS.md`もあるので先に読んでください。**

## 今回の目的

U-FRET（https://ufret.jp）のような「歌詞の上にコードを表示しながら自動スクロールして弾く」機能を、既存の「曲」タブ（`songChordInfo`で弾ける曲を判定する既存画面）に追加する。アイデアメモ（Obsidian Vault/メモ/アイデア.md）の01番の要望が発端。

**現在の状態: `index.html`本体への統合まで完了・実機(iPhone16)で一通り確認済み。** 2026-08-14に、まず`song-player-test.html`という単体プロトタイプファイルでUI/UXを検証し、その後同じ内容を`index.html`に統合した。PCプレビューでの機能テストは全て実施済み。実機での最終確認はユーザーが実施し「一通り良さそう」との反応を得ている。

## 実装済みの機能（`index.html`）

### データモデル
- `USER_SONGS`（localStorageキー`guitar-user-songs`、プロフィール非依存＝端末内で共通）: ユーザーが追加した曲。`{id, title, artist, lyrics:string[], tags:{line,ch,chord}[], progression, strumPattern:string[]}`。
- 既存の`SONGS`（ハードコード、コードのみ）と`mergedSongs()`でマージして「曲」タブに表示。`isUser`フラグで区別。
- `progression`は`tags`の配置順から`deriveProgression()`で自動導出（現状は「全体」1セクションにまとめる簡易版。セクション分けは未実装、下記「今後の課題」参照）。
- 「弾ける」判定に使う`chordStatus`は今まで通りプロフィール単位のまま変更していない。

### 「曲」タブの変更
- 「＋ 新しい曲を追加（歌詞＋コード）」ボタン→曲名・アーティスト名を聞くモーダル→作成後すぐ編集画面へ。
- 「📋 曲データを貼り戻す」ボタン→JSON貼り付けで曲を復元。壊れたJSON・曲名なし・不正な形式のタグは弾いてエラー表示。iOSの「メモ」等のスマート引用符（`"``"`）による文字化けは読み込み前に自動で普通の引用符へ正規化してから`JSON.parse`する。
- ユーザー追加曲の一覧行に「🎵 演奏あり」バッジ（歌詞・コード配置がある場合）、詳細を開くと「▶ 演奏」「✎ 歌詞を編集」「🗑 削除」ボタン。
- コード0件のユーザー曲は「弾ける✓」ではなく「未入力」バッジにして誤解を防いでいる。
- 曲名・アーティスト名（ユーザー入力）は`escapeHtml()`でエスケープしてから`innerHTML`に埋め込む（過去のプロフィール名エスケープ漏れバグと同じ問題を作らないため）。

### 歌詞＋コード編集画面（`sp-edit-overlay`、`song-player-test.html`から移植）
- 歌詞ペースト/入力で自動反映（300msデバウンス）。文字タップ→画面下固定パレット（`ALL_CHORDS`の26種類）からタップで配置。配置済みタグはタップで選択→「◀/▶/削除/選択解除」ボタンで編集。
- 文字ごとに「コード表示スロット＋文字」を1つのinline-flexユニットにまとめる方式（`spBuildLineEl`）。行の折り返し・空行(間奏)・行末へのコード配置に対応。
- 歌詞編集で内容が変わった行のタグは自動削除し通知（行単位の完全一致比較、LCS等の高度な差分は不採用）。
- 「この曲のデータをコピー」でJSON化してクリップボードにコピー（バックアップ用）。
- **ストロークパターン（新規、2026-08-14追加）**: 曲全体で共通の1パターンのみ（行/セクションごとの個別設定は未実装、下記参照）。↓/↑/✕(ミュート)ボタンをタップした順に追記していく方式。並べ替えはできず「1つ戻す」「全部クリア」のみ。演奏画面にもそのまま表示（アニメーションなし）。

### 演奏画面（`sp-play-overlay`）
- `scrollTop`直接操作は整数pxに丸められ遅い速度でガクつくため、CSS `transform`でサブピクセル単位に動かす方式。一時停止時はネイティブ`scrollTop`に焼き込み、手動スワイプを継続できるようにする。
- 描画駆動は`requestAnimationFrame`(60fps)主駆動＋`setInterval`(400ms)の保険による二重駆動（非表示タブでrAFが完全停止する既知の罠への対策、既存の「流れる」タブと同じパターン）。
- `visibilitychange`で、画面ロック等でタブが非表示だった時間ぶん起点時刻を後ろにずらし、復帰時に「経過時間ぶん一気に進む」ことを防いでいる。
- 手動速度は「秒/行」という単位（±ステッパー＋ゆっくり/ふつう/はやいプリセット）。BPM連動モードは「1拍の移動量(px)」との組み合わせ。
- 全ボタン・タップ要素に`touch-action:manipulation`（連打によるダブルタップズーム誤爆対策）。テキスト入力欄はfont-size 16px（iOS自動ズーム対策）。

### 著作権への対応
- 歌詞データはユーザー本人が用意する（コピペ・手入力・スクリーンショット+iOS Live Text等）。Claudeが歌詞を取得・転記することは一切していない。
- 曲データは`localStorage`止まりで、公開リポジトリ（GitHub Pages配信中）にコミットされる`index.html`自体には実データを含めていない（ロジックのみ）。

## 今後の課題（未着手、優先度は特に決めていない）

1. **ストロークパターンの個別設定**: 現状は曲全体で1パターンのみ。行/セクションごとに変えたい場合、今のデータモデルには「セクション」概念自体が無い（`progression`は常に「全体」1つの塊）ため、まず行単位かセクション単位かの設計判断が必要（ユーザーとの会話で保留にした項目）。
2. **画像からのストロークパターン変換**: ユーザーから提案があったが、著作権上の懸念（楽譜画像の転記になる）とOCR実装の重さから見送り。手動での自由入力（実装済み）でカバーする方針。
3. 全曲まとめての書き出し/読み込み（今は1曲ずつの「コピー」「貼り戻す」のみ）。
4. アクセシビリティ（タップ領域、読み上げ対応）はCodexの調査で指摘があったが未対応。

## Codex活用状況（2026-08-14）

`codex-implementer`エージェント経由で`song-player-test.html`（プロトタイプ段階）の追加バグ調査を1回実施。8件の小さなバグ（touch-actionのCSSセレクタミス、iOS自動ズーム対策、絵文字/サロゲートペア対応、タブ切替時の状態整合性など）をCodexが直接修正し、Claude側で全て動作再確認済み。設計判断が必要な項目（歌詞編集後のズレ、バックグラウンド復帰時の補正）は別途Claudeが対応し確認済み。本実装(`index.html`統合)自体はClaudeが直接行った。

## 参照

- [CLAUDE.md](CLAUDE.md)
- [AGENTS.md](AGENTS.md)
- `song-player-test.html`（プロトタイプ。実験用のダミーテキストのみを含む、実装検証の記録として残置）
- `index.html`（`SONGS`/`USER_SONGS`/`mergedSongs`/`songChordInfo`/`songChords`/`ALL_CHORDS`、`sp*`関数群が歌詞＋コード演奏機能一式）
- Obsidian Vault/メモ/アイデア.md（01番、今回の発端となったU-FRET関連の要望）
