# HANDOFF.md（guitar-practice）

- **最終更新日時**: 2026-08-18（Claude追記）
- **変更主体**: Claude（演奏画面をカード表示に作り直し・per-chordストローク追加）

このファイルは長期の設計方針（`CLAUDE.md`）ではなく、今どこまで進んでいて次に何をするかの短期メモ。ClaudeとCodexの併用運用の一環（詳細はルート`CLAUDE.md`の「Claude/Codexの併用について」、`Obsidian Vault\メモ\Claude-Codex併用ルール.md`を参照）。**このリポジトリには`AGENTS.md`もあるので先に読んでください。**

## 今回の目的

U-FRET（https://ufret.jp）のような「歌詞の上にコードを表示しながら弾く」機能を、既存の「曲」タブに追加する。アイデアメモ（Obsidian Vault/メモ/アイデア.md）の01番の要望が発端。2026-08-14に本実装まで完了・実機確認済み。2026-08-18に、ユーザーが実際に見ていた参考画像（フレット図＋ストローク矢印＋歌詞が横に並び、現在位置がハイライトされるスタイル）に合わせて演奏画面を作り直した。

**現在の状態: 一通り実装・PC上での機能テスト完了、pushもGitHub Pagesまで到達済み。実機での最終確認は次回。**

## 実装済みの機能（`index.html`）

### データモデル
- `USER_SONGS`（localStorageキー`guitar-user-songs`、プロフィール非依存＝端末内で共通）: `{id, title, artist, lyrics:string[], tags:{line,ch,chord,strum?}[], progression, strumPattern:string[]}`。
- 既存の`SONGS`（ハードコード、コードのみ）と`mergedSongs()`でマージして「曲」タブに表示。`isUser`フラグで区別。
- `progression`は`tags`の配置順から`deriveProgression()`で自動導出（現状「全体」1セクションのみ）。
- **`tag.strum`（2026-08-18追加）**: 配置済みコードタグ1つ1つに、個別のストロークパターンを持たせられる。未設定のタグは`spResolveStrumAt()`で「手前で最後にstrumが設定されたタグ→無ければ曲全体の`strumPattern`」の順にフォールバックして継承する。サビ前などコード（＝ストローク）が変わる瞬間に対応するため。

### 「曲」タブ・編集画面（歌詞＋コード＋ストローク入力）
- 「＋新しい曲を追加」「📋 曲データを貼り戻す」「✎ 曲名を編集」「🎵 演奏あり」バッジなど、曲の追加・編集・復元一式。詳細は過去の履歴（2026-08-14分のコミットログ）を参照。
- 曲全体の初期ストロークパターン（↓/↑/✕/〜ボタン）に加え、**配置済みタグを選択した時に「このコードからのストローク」を個別設定できる欄が出る**（`sp-tag-strum-controls`）。設定済みのタグは歌詞プレビュー上で枠線が付いて視覚的にわかる（`.has-strum`）。

### 演奏画面（`sp-play-overlay`、2026-08-18に全面作り直し）
- **以前の「縦スクロールでテキストの歌詞+コード名が流れる」表示は廃止**し、既存の「流れる」タブと同じ**タイル使い回し方式**（常に4枚のDOM、中身だけ差し替え）で、**フレット図つきのコードカードが横に並ぶ**表示に置き換えた。
- 1カード＝配置済みタグ1つ。フレット図（既存`ALL_CHORDS`＋`drawFret()`をそのまま再利用）、そのタグ位置で有効なストローク（`spResolveStrumAt`で継承込みに解決）、次のタグまでの歌詞の断片、を表示。
- **自動再生（タイマーで一定間隔ごとに1カード進める）と手動送り（◀/▶ボタン）を両方いつでも使える**。自動再生は「秒指定」「BPM+何拍で切替か」の2モード。曲の最後まで来たら自動で止まる。
- 旧実装にあった`requestAnimationFrame`/`transform`でのサブピクセルスクロール、`visibilitychange`補正等は、連続スクロールをやめてタイマーでの離散的な1カード送りに変えたため不要になり削除した。

### 削除した機能: 参考動画添付（2026-08-18、追加した直後に撤回）
- 「演奏画面に動画を乗せたい」という要望と誤解して実装したが、ユーザーの意図は「動画のような見た目（フレット図+ストローク+歌詞の表示スタイル）を自分たちのデータで再現したい」であり、動画そのものの添付は要望されていなかった。git revertで削除済み。**IndexedDBでの動画保存という設計自体は実装・動作確認までしたので、将来的に本当に動画添付が必要になった時は該当コミット(`6b52b65`とそのrevert `3bfd2c3`)を参照すると再利用できる。**

### 著作権への対応（変更なし）
- 歌詞データはユーザー本人が用意する。Claudeが歌詞を取得・転記することは一切していない。
- 曲データは`localStorage`止まりで、公開リポジトリにコミットされる`index.html`自体には実データを含めていない。

## 今後の課題（未着手）

1. 全曲まとめての書き出し/読み込み（今は1曲ずつの「コピー」「貼り戻す」のみ）。
2. アクセシビリティ（タップ領域、読み上げ対応）。
3. 演奏画面のカード送りアニメーション（今は瞬時に切り替わるだけ。「流れる」タブにある「もうすぐ切り替わる」時の滑らかな移動は今回は簡略化のため見送った）。

## Codex活用状況（2026-08-14, 2026-08-18）

- 2026-08-14: `song-player-test.html`（プロトタイプ段階）の追加バグ調査を1回実施。8件の小さなバグをCodexが直接修正し、Claude側で確認済み。
- 2026-08-18: 参考動画添付機能の実装をCodexに委譲したが、**シェル実行禁止の制約で既存コードを検索できず、既存関数を直接編集せずモンキーパッチする方式になり、かつUI挿入先のCSSクラスが実在せず意図しない位置に挿入される**という問題が生じたため、Codexの差分は不採用・破棄（git checkoutで取り消し）。この機能自体も後に要望と異なると判明し撤回。**教訓: シェル禁止の制約下でCodexに大きめの機能追加を任せる場合、既存コードの構造（関数名・DOM構造・CSSクラス名）を事前にプロンプト内で具体的に示さないと、検索できない代わりに安全側だが構造の合わない実装になりやすい。**

## 参照

- [CLAUDE.md](CLAUDE.md)
- [AGENTS.md](AGENTS.md)
- `index.html`（`SONGS`/`USER_SONGS`/`mergedSongs`/`songChordInfo`/`songChords`/`ALL_CHORDS`/`drawFret`、`sp*`関数群が歌詞＋コード＋ストローク演奏機能一式）
- Obsidian Vault/メモ/アイデア.md（01番、今回の発端となったU-FRET関連の要望）
